# TraceQuant v2 bootstrap defines no runtime environment variables.
#
# Persistent roots, trading modes, venue connectivity, and Live configuration
# are intentionally unavailable until separately scoped capabilities add typed,
# fail-closed configuration contracts. Never place credentials in this file.
