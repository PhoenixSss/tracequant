# TraceQuant Runtime

Shadow, Demo, and Live runtime entry points belong here.

Runtime must keep execution modes explicit, keep live trading disabled by default, apply fail-closed risk gates, and keep exchange-specific behavior behind adapters.

No new live capability is introduced by this scaffold.
