# Editable visual sources

- `static-command-paths.csv`: per-Skill before/after source and legacy-fragment metrics.
- `skill-runner-flow.mmd`: fixed authority map, bounded expansion, and write/manual gates.

These are source-level materials, not runtime Token evidence.
